package collections;

/**
 * Функциональный интерфейс для элементов списка
 * @author Солодков Владимир
 * @version 1.0.0
 */
public interface ForEach {
    /**
     * Функция для работы с одним элементом списка
     * @param item элемент списка
     */
    public void fn(Object item);
}
